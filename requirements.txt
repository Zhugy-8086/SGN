# Runtime dependencies -- mirror of pyproject.toml [project.dependencies]
numpy>=1.24.0

# Test dependencies -- mirror of pyproject.toml [project.optional-dependencies].dev
pytest>=7.4.0
pytest-cov>=4.1.0

# Build dependency -- pybind11 for CMake pyd build
pybind11>=2.11.0
